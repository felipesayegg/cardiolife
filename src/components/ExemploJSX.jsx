export default function ExemploJSX() {
  const titulo = "CardioLife";
  const descricao = "Aplicação inicial para estudo de ReactJS e medicina";

  return (
    <section>
      <h1>Olá, {titulo}!</h1>
      <p>{descricao}</p>
    </section>
  );
}